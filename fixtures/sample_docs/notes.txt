Vector stores in this project use numpy arrays plus JSONL metadata.
Model weights live under models/all-MiniLM-L6-v2/model.onnx (~87MB).
Shard utilities split oversized weight files for GitHub's 100MB limit.
